<!DOCTYPE html>
<html>
<head>
  <title>Three-Lights.net Gallery</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <h1>Three-Lights.net Gallery</h1>

  <% if is_admin? %>
    <p class="admin">Currently logged in as <strong>Admin</strong></p>
  <% end %>

  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>

<%= yield %>

</body>
</html>
